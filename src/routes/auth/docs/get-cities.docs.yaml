/auth/get-cities/:
  get:
    summary: api to get cities.
    tags:
    - Authentication
    description: auth
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: state
      in: query
      type: string
      description: iso code of state - optional
      example: IN-01
    - name: stateName
      in: query
      type: string
      description: name of state - optional  
    responses:
      200:
        description: "successful attempt, returns `success message`"