/case/status-count:
  get:
    security:
    - Bearer: []
    summary: api for case percent.
    tags:
    - Authentication
    description: api for case percent.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: Query
      description: ''
      in: query
      required: true    
    responses:
      200:
        description: "successful attempt, returns `success message`"