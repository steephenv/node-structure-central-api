/case/fetch-case?caseId=xxxxxxxxxxxxxx:
  get:
    security:
    - Bearer: []
    summary: api for fetch case.
    tags:
    - Authentication
    description: api for fetch case.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: Query
      description: ''
      in: query
      required: true    
    responses:
      200:
        description: "successful attempt, returns `success message`"