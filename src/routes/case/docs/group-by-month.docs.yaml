/case/group-by-month:
  get:
    security:
    - Bearer: []
    summary: api for case group by month.
    tags:
    - Authentication
    description: api for case group by month.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest 
    responses:
      200:
        description: "successful attempt, returns `success message`"