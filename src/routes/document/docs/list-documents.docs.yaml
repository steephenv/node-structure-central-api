/documents/list-folders?caseId:'1111111111111111':
  get:
    # security:
    # - Bearer: []
    summary: api for listing documents.
    tags:
    - Authentication
    description: api for listing documents.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: Query
      description: ''
      in: query
      required: true    
    responses:
      200:
        description: "successful attempt, returns `success message`"