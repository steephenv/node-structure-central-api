/documents/move-doc:
  post:
    # security:
    # - Bearer: []
    summary: api for copying documents in documents.
    tags:
    - Authentication
    description: api for copying documents in documents.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: Body
      description: ''
      in: body
      required: true
      schema:
        type: object
        required:
        - destinationUrl
        - docType
        - docId
        properties:
          destinationUrl:
            type: string
            example: '/test-images/new'
          docType:
            type: string
            example: 5c10d160a258192a68f958ab
          docId:
            type: string
            example: 5c10d160a258192a68f958ab       
    responses:
      200:
        description: "successful attempt, returns `success message`"