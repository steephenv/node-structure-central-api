/documents/create-doc:
  post:
    # security:
    # - Bearer: []
    summary: api for updating documents.
    tags:
    - Authentication
    description: api for updating documents.
    parameters:
    - name: X-Requested-With
      description: set value to XMLHttpRequest if you are making an xhr request
      in: header
      required: true
      default: XMLHttpRequest
    - name: Body
      description: ''
      in: body
      required: true
      schema:
        type: object
        required:
        - docTitle
        properties:          
          accessRights:
            type: array
          case:
            type: string
          recordType:
            type: string
          docTitle:
            type: string
          docUrl:
            type: string
          docCategory:
            type: string
          docStage:
            type: string
          docSummary:
            type: string
          documentDate:
            type: string
          pageCount:
            type: string
          secretary:
            type: string
          sentBy:
            type: string
          sentByOrg:
            type: string
          ward:
            type: string
          receiveSection:
            type: string
          physicalLoc:
            type: string
          lastAccessBy:
            type: string
          fileType:
            type: string
          accessLog:
            type: string
          atoll:
            type: string
          island:
            type: string
          code :
            type: string         
    responses:
      200:
        description: "successful attempt, returns `success message`"